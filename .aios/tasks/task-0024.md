---
schema: aios.task/v1
id: task-0024
project: aios-lab
title: Define routing inventory and workload evidence
state: done
retry:
  count: 2
  limit: 2
approval: not_required
last_review: review-0037
---

# Define routing inventory and workload evidence

## Objective

Add a backward-compatible routing configuration contract and a deterministic
provider-neutral workload-context builder. Operators can declare the exact
Claude, Codex, or other command candidates that actually exist, their ordered
capability tiers, Role/capability eligibility, budgets, distribution targets,
and bounded recovery limits without putting any provider or model field into a
Task. Given a Task and active Role, the context builder exposes inspectable
planning/profile/complexity/risk/context/tool/verification/history evidence for
later policy selection and defaults conservatively when evidence is absent.

## Acceptance Criteria

- A strict `aios.routing/v1` parser accepts a versioned JSON configuration with
  exactly these conceptual groups: an ordered tier definition; a candidate
  catalog; policy limits and provider distribution targets; optional
  provider-neutral workload hints selected by Task id or adopted plan id; and
  optional validated operator-override selectors whose application is deferred
  to the operator-control surface.
  Unknown top-level or nested fields are rejected with a path-specific error.
- Every candidate declares a stable candidate id, provider id, exact model id,
  tier, eligible Roles, non-empty command argv, enabled state, context/tool
  capabilities, and cost/latency class. Candidate ids are unique; referenced
  tiers and capabilities must exist; argv uses the existing no-shell validation;
  weights and limits are finite positive values; duplicate provider/model/Role
  entries and an empty eligible catalog are rejected.
- Model identifiers and capabilities come only from the configuration. No
  source default silently invents a current Claude or Codex model, derives a
  tier from a model-name substring, probes a remote model list, or places a
  user-local executable path in repository code.
- The configuration can express at least two provider families and tests use
  explicit Claude and Codex fixtures at lower and higher tiers. Provider ids
  remain data rather than a hard-coded two-value enum so compatible command
  Workers are not excluded.
- A workload-context builder receives the complete validated Task, active Role,
  repository root, routing config, and optional Review/session history and
  returns a normalized immutable object containing: Task id; Role; work kind
  (`planning`, `implementation`, or `unknown`); parent plan id/profile when
  unambiguously discoverable; complexity and risk bands; Task-context size;
  required capabilities/tools; verification burden; latency and cost budget;
  approval/retry state; relevant prior outcome counters; uncertainty flags; and
  a source label for every non-default value.
- Parent-plan discovery reuses validated adopted-plan inspection and succeeds
  only when exactly one adopted plan names the Task. Zero or multiple matches
  are recorded as uncertainty rather than guessed. A malformed plan fails that
  assessment closed without making a lower tier eligible.
- `work_kind: planning` is established only by an explicit provider-neutral
  hint or a strict plan-only Task contract (an Objective naming its plan output,
  a constraint limiting writes to that plan directory, and a required
  non-mutating `adopt --check` verification). A title containing "plan" alone
  is insufficient. Conflicting evidence becomes `unknown` and is marked
  conservative.
- Complexity, risk, context, and verification rules are deterministic and
  documented. They may use explicit hints, plan profile, Task byte/section
  counts, approval requirement, retry/Review history, declared capabilities,
  and named verification commands; they do not call an AI model or apply an
  opaque score. Missing semantic hints raise uncertainty/risk and can never be
  used as evidence for a lower tier.
- A planning workload and any workload whose safety-critical evidence is
  unknown carries a normalized `minimum_tier` of the configured high tier. A
  lower-tier minimum is emitted only for a non-planning, bounded context with
  low complexity, low risk, known tool requirements, objective verification,
  and no unresolved failure history. The exact gate and rejection reasons are
  present in the normalized context.
- Configuration loading preserves `aios.assignments/v1` byte-for-byte behavior:
  the existing Role-only file still produces the same `FileAssignmentResolver`,
  is re-read before each Role action, records sessions as before, and emits no
  fabricated routing-decision evidence. Routing activates only when the input
  schema is `aios.routing/v1`.
- Unit tests cover every validation rule, legacy Assignment compatibility,
  planning detection and title-only rejection, adopted/ambiguous/malformed plan
  context, high-tier conservative defaults, lower-tier gate pass/fail cases,
  prior failure/review evidence, and stable normalized output for identical
  repository state.

## Constraints

- Do not change Task, Review, Plan, Result, or Worker-execution document
  schemas. Workload hints live in routing configuration and remain provider and
  model neutral.
- Do not launch a Worker, choose a candidate, write a decision ledger, change
  the Loop Engine, add CLI flags, or render dashboard UI in this Task.
- Reuse existing Assignment argv validation, plan inspection, Task loading, and
  session validation rather than creating alternate document parsers.
- Do not add a provider SDK, network discovery, credential handling, daemon,
  concurrency, or dependencies.

## Context

`src/workers.js` currently validates `aios.assignments/v1` and
`FileAssignmentResolver.resolve(role)` returns one `CommandWorker` after
re-reading the file. `src/plans.js` already validates adopted plans and their
profiles, while Tasks and Reviews already expose approval, retry, and prior
outcome evidence. This Task establishes the explicit inventory and normalized
inputs needed to evolve resolution safely without changing those durable
documents.

## Attempts

<!-- aios:attempt-frame v1 number=1 summary=1252 verification=961 -->
### Attempt 1

#### Summary

Added src/routing.js with a strict, deeply frozen aios.routing/v1 contract for ordered tiers, declared capabilities and cost/latency classes, explicit Claude/Codex-or-other command candidates, enabled availability, provider targets, bounded recovery limits, provider-neutral Task/plan hints, and candidate-id overrides. Validation rejects unknown fields with exact paths, bad references, duplicate provider/model/Role entries, invalid argv, weights, limits, selectors, and empty enabled Role catalogs; model names and capabilities are never inferred. Added legacy-versus-routing config loading without changing FileAssignmentResolver behavior. Added deterministic workload context construction using the existing adopted-plan order and exported plan-metadata validator, strict plan-only detection rather than title guessing, structural/context measurements, supplied Review/session counters, complete source labels, and conservative high-tier defaults. Lower tier is available only to an explicitly hinted low-complexity, low-risk, bounded Implementer with known capabilities, objective verification, no required approval, no retry/failure history, and no safety-critical uncertainty. Exported the new public APIs and added comprehensive routing tests.

#### Verification

`node --test test/routing.test.js`: 13 passed, 0 failed, covering explicit Claude/Codex lower/high catalogs, every catalog/reference/argv/weight/limit family, hint and override validation, legacy Assignment preservation, strict and explicit planning detection, title-only rejection, conflicting evidence, lower-tier pass and fail gates, required approval and retry fail-closed behavior, Review/session history, single/ambiguous/malformed/invalid-header parent plans, stable immutable output, and source labels. `npm test`: 212 passed, 0 failed. `git diff --check`: passed. The implementation changes only src/routing.js, src/index.js, src/plans.js (exporting the existing validator), src/workers.js (exporting the existing argv validator), and test/routing.test.js; it adds no Worker launch, selection, decision ledger, engine/CLI/dashboard behavior, dependency, provider SDK, or network call, and leaves the local .aios/assignments.json modification untouched.
<!-- /aios:attempt-frame v1 number=1 -->

<!-- aios:attempt-frame v1 number=2 summary=1306 verification=1054 -->
### Attempt 2

#### Summary

Corrected every finding in review-0035. Adopted parent discovery now reuses exported existing validators for exact plan metadata and every required PLAN body section before readPlanOrder validates adopted Execution Order, Tasks, and projects; parseable plans missing Brief or carrying invalid headers fail high. planningContract now requires one exact inline `node src/cli.js adopt plans/<same-id> --check` or `npm run aios -- adopt plans/<same-id> --check` command plus a standalone affirmative list item limiting writes only to that same directory, rejecting split commands, wrong paths, and negative prose. Review history now passes validateReviewMetadata with a non-empty body and matching project, and session history passes the exported existing SessionLedger-row validator; any malformed history is diagnosed and forces high tier. Added source attribution for uncertainty flags, lower-tier eligibility/rejections, and diagnostics. Documented the config groups, thresholds, evidence precedence, planning contract, parent-plan uncertainty, history contract, and exact lower-tier gate in README. Expanded validation and workload tests to cover the previously missing scalar, enum, reference, duplicate, unknown nested-field, false-positive planning, malformed history, and malformed plan-section cases.

#### Verification

`node --test test/routing.test.js`: 17 passed, 0 failed. New regression cases independently prove missing required PLAN sections and invalid headers fail closed; split adopt/--check fragments, wrong paths, and negative write prose are not planning; actual contract-valid Review and SessionLedger rows produce counters; malformed histories force high tier; and generated uncertainty/lower-tier/diagnostic evidence has source labels. The validation matrix now covers enabled/context/Role fields, duplicate catalogs and provider targets, high-tier/default-budget references, hint selectors/enums/budgets, override candidate/boolean checks, and unknown fields in tiers, policy, limits, budgets, targets, hints/selectors, and overrides/selectors. `npm test`: 216 passed, 0 failed. `git diff --check`: passed. README records all deterministic thresholds and precedence. No Worker launch, selection, routing ledger, LoopEngine, CLI, dashboard, dependency, provider SDK, network, Task/Review/Plan/Result schema, or local Assignment behavior was added or changed.
<!-- /aios:attempt-frame v1 number=2 -->

<!-- aios:attempt-frame v1 number=3 summary=1041 verification=815 -->
### Attempt 3

#### Summary

Corrected all three findings in review-0036 and tightened the boundary further. Planning detection now reads only the first normalized Objective paragraph, requires it to begin with an affirmative output verb and name a plan/proposals under one exact plans/<id> directory, rejects negative and inspection/status/report/analysis wording before the output noun, and still requires an exact same-path inline adopt --check command plus one affirmative directory-only Constraints item. Soft-wrapped Markdown inside the same Objective paragraph or Constraints list item is supported and regression-tested. Parent discovery now inspects every plan in sorted directory order: a plan with any P-## placeholder remaining while its Execution Order contains a task-#### is recorded as a malformed partially adopted plan, not tolerated as a missing parent. Plan matches and diagnostics errors are sorted before normalization, making output independent of readdir order. README documents the affirmative output, hybrid-plan, and deterministic-order rules.

#### Verification

`node --test test/routing.test.js`: 19 passed, 0 failed. New regression evidence proves Do-not-produce, inspection-only, Produce-no-plan, and inspection-report Objectives do not qualify; a real soft-wrapped reviewed software-feature plan Objective and write-only item do qualify; a hybrid P-##/task-#### plan forces parent_plan_invalid and high tier; and multiple a/m/z plan errors are lexically sorted with two builds producing deeply identical contexts. All 17 prior contract, validation, history, plan-structure, source-label, and lower-tier tests still pass. `npm test`: 218 passed, 0 failed. `git diff --check`: passed. No Worker launch, selection, routing ledger, LoopEngine, CLI, dashboard, dependency, provider SDK, network, Task/Review/Plan/Result schema, or local Assignment behavior was added or changed.
<!-- /aios:attempt-frame v1 number=3 -->
