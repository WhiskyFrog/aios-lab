---
schema: aios.task/v1
id: task-0035
project: aios-lab
title: Make provider capacity outages and routing-policy changes operationally recovera
state: done
retry:
  count: 0
  limit: 2
approval: approved
last_review: review-0051
---

# Plan capacity-recovery-operations

## Objective

Produce a reviewed plan under `plans/capacity-recovery-operations/` in this repository for the Brief below. Follow the existing AIOS Planner protocol, preserve the Brief, choose the narrowest applicable registered profile, and keep execution order only in the plan document.

## Brief

Make provider capacity outages and routing-policy changes operationally recoverable, addressing findings 2 through 5 of reports/dogfooding-whisky-frog-bundle1.md. First, a provider-reported usage-limit failure that the worker cannot corroborate through structured provider evidence (for example a plain 'You have hit your usage limit ... try again at <date>' error line) must surface as a structured capacity event carrying the provider-supplied reset time, not as a hard worker error that consumes the action's single fallback. Second, persist a per-candidate capacity cooldown (candidate id, retry_at, evidence) in machine-local runtime state so adaptive-routing selection skips cooled-down candidates until retry_at passes, records the skip as auditable gate evidence, expires cooldowns automatically, and gives the operator commands to list and clear them early when a quota resets ahead of schedule. Third, add an operator recovery command that resets the recorded routing decision rows for exactly one action key (task, role, attempt) whose steps are all non-terminal or failed, leaving the ledger schema-valid including its updated_at invariant and an auditable trace of the reset, so routing configuration edits between runs no longer strand actions on a policy-revision conflict with no remedy short of hand-editing runtime/routing-decisions.json. Preserve the fail-closed philosophy throughout: recorded decisions stay immutable, hard safety gates are never weakened, and every new state is validated on load. Verify with unit tests over fakes and fixture ledgers only; no provider calls and no live sessions.

## Planner Profiles

- `generic-goal`: Separate the goal into independently verifiable outcomes. Define observable completion evidence for every outcome.
- `software-feature`: Separate contracts, implementation, tests, and integration. Require automated checks and observable feature behavior.
- `bug-fix`: Separate reproduction, root cause, correction, and regression coverage. Prove the original failure and the regression test both behave correctly.
- `website`: Separate information architecture, shared visual system, pages, and quality checks. Cover content, responsive layouts, accessibility, and navigation.
- `research`: Separate questions, source collection, corroboration, and synthesis. Require source traceability and explicit uncertainty.
- `migration`: Separate preflight, backup, conversion, validation, and rollback readiness. Require integrity checks and a tested recovery boundary.
- `content`: Separate audience and purpose, outline, draft, fact-checking, and editing. Require factual, structural, and editorial acceptance checks.

## Acceptance Criteria

- `plans/capacity-recovery-operations/PLAN.md` has valid `aios.plan/v1` front matter with a registered profile and a non-empty `profile_reason`.
- `PLAN.md` contains non-empty Brief, Profile Application, Assumptions and Risks, Decomposition Rationale, and Execution Order sections.
- Proposal files are contiguous valid Task documents using `P-01`, `P-02`, and so on, with relationships and ordering stated only in `PLAN.md`.
- Implementer Verification records a passing, non-mutating `aios adopt plans/capacity-recovery-operations --root . --check` run.

## Constraints

- Write only under `plans/capacity-recovery-operations/`.
- Do not modify `.aios/`, existing Tasks, Reviews, configuration, or ledgers.
- Do not dispatch another Worker or spend provider capacity from the plan.
- Proposals become Tasks only through a separate operator-invoked `aios adopt` command.
- Do not include provider/model requirements, credentials, secrets, or user-local paths.

## Context

The Planner proposes independently verifiable, one-session outcomes. Review checks both mechanical plan validity and decomposition quality; adoption remains an explicit operator action.

## Attempts

<!-- aios:attempt-frame v1 number=1 summary=944 verification=363 -->
### Attempt 1

#### Summary

Produced plans/capacity-recovery-operations/ (PLAN.md + P-01..P-04) implementing the software-feature profile for the Brief (dogfooding findings 2-5): P-01 widens Codex Worker capacity corroboration to accept a bounded, fail-closed textual parse of provider usage-limit prose when the structured app-server probe can't corroborate; P-02 defines a validated aios.candidate-cooldowns/v1 record and a new non-overridable hard gate added symmetrically to routing-policy.js and routing-ledger.js; P-03 wires cooldown persistence/expiry into routed dispatch and adds `aios route cooldowns`/`aios route clear-cooldown`; P-04 adds `aios route reset <task>:<role>:<attempt>` to supersede (never rewrite) stranded non-terminal-or-failed decision rows so a policy-revision change no longer requires hand-editing routing-decisions.json. Every proposal is self-contained (no cross-proposal references); Execution Order and dependencies live only in PLAN.md.

#### Verification

Ran `node src/cli.js adopt plans/capacity-recovery-operations --root . --check` and the npm-script equivalent (`npm run aios -- adopt ... --check`); both returned {"kind":"checked","profile":"software-feature","proposals":["P-01","P-02","P-03","P-04"]}. Confirmed via `git status` before/after that the --check run made no writes anywhere, including under .aios/.
<!-- /aios:attempt-frame v1 number=1 -->
