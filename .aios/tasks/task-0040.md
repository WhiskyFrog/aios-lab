---
schema: aios.task/v1
id: task-0040
project: aios-lab
title: Fix the reviewed route-reset workflow so a real aios run/progress can record a f
state: done
retry:
  count: 0
  limit: 2
approval: approved
last_review: review-0057
---

# Plan route-reset-distribution-recovery

## Objective

Produce a reviewed plan under `plans/route-reset-distribution-recovery/` in this repository for the Brief below. Follow the existing AIOS Planner protocol, preserve the Brief, choose the narrowest applicable registered profile, and keep execution order only in the plan document.

## Brief

Fix the reviewed route-reset workflow so a real aios run/progress can record a fresh step-0 decision after the prior action generation is superseded and the routing policy revision changes. Reproduce the observed failure 'distribution.observed: must equal the actual preceding ledger window' with a fixture containing ordinary preceding decisions plus a genuine resetAction-produced superseded generation, make selection history projection and routing-ledger distribution-window replay use one consistent documented generation/counting rule without deleting or rewriting history, preserve immutable audit rows and fail-closed validation, and add regression coverage that exercises the real routed dispatch/record path after reset. Also cover or explicitly prevent same-policy exact-key collisions so the CLI contract cannot claim a fresh sequence that record rejects. Use fixture tests only; no provider calls or live sessions.

## Planner Profiles

- `generic-goal`: Separate the goal into independently verifiable outcomes. Define observable completion evidence for every outcome.
- `software-feature`: Separate contracts, implementation, tests, and integration. Require automated checks and observable feature behavior.
- `bug-fix`: Separate reproduction, root cause, correction, and regression coverage. Prove the original failure and the regression test both behave correctly.
- `website`: Separate information architecture, shared visual system, pages, and quality checks. Cover content, responsive layouts, accessibility, and navigation.
- `research`: Separate questions, source collection, corroboration, and synthesis. Require source traceability and explicit uncertainty.
- `migration`: Separate preflight, backup, conversion, validation, and rollback readiness. Require integrity checks and a tested recovery boundary.
- `content`: Separate audience and purpose, outline, draft, fact-checking, and editing. Require factual, structural, and editorial acceptance checks.

## Acceptance Criteria

- `plans/route-reset-distribution-recovery/PLAN.md` has valid `aios.plan/v1` front matter with a registered profile and a non-empty `profile_reason`.
- `PLAN.md` contains non-empty Brief, Profile Application, Assumptions and Risks, Decomposition Rationale, and Execution Order sections.
- Proposal files are contiguous valid Task documents using `P-01`, `P-02`, and so on, with relationships and ordering stated only in `PLAN.md`.
- Implementer Verification records a passing, non-mutating `aios adopt plans/route-reset-distribution-recovery --root . --check` run.

## Constraints

- Write only under `plans/route-reset-distribution-recovery/`.
- Do not modify `.aios/`, existing Tasks, Reviews, configuration, or ledgers.
- Do not dispatch another Worker or spend provider capacity from the plan.
- Proposals become Tasks only through a separate operator-invoked `aios adopt` command.
- Do not include provider/model requirements, credentials, secrets, or user-local paths.

## Context

The Planner proposes independently verifiable, one-session outcomes. Review checks both mechanical plan validity and decomposition quality; adoption remains an explicit operator action.

## Attempts

<!-- aios:attempt-frame v1 number=1 summary=1363 verification=476 -->
### Attempt 1

#### Summary

Produced a reviewed AIOS plan under plans/route-reset-distribution-recovery/ for the route-reset distribution-recovery Brief. PLAN.md uses profile: bug-fix (profile_reason ties it to the Brief's named reproduction target and reproduction/root-cause/correction/regression-coverage shape) with non-empty Brief, Profile Application, Assumptions and Risks, Decomposition Rationale, and Execution Order sections. Two contiguous proposals, P-01.md and P-02.md, are valid aios.task/v1 documents with no cross-proposal references (ordering lives only in PLAN.md): P-01 reproduces and corrects the reported 'distribution.observed: must equal the actual preceding ledger window' failure by reconciling historyProjection's generation-aware window (src/routing-dispatch.js) with validateLedger's generation-unaware distribution replay (src/routing-ledger.js ~line 1140); P-02 reproduces and corrects the related but distinct same-policy exact-key collision where record()'s findRecordIndex (src/routing-ledger.js ~line 1278) can mistake a superseded generation's row for the 'existing' row of a fresh post-reset selection when the policy revision is unchanged. Root-cause claims were verified by direct code reading against src/routing-ledger.js, src/routing-dispatch.js, src/routing-policy.js, and the prior review history in .aios/reviews/review-0055.md and review-0056.md.

#### Verification

Ran `node src/cli.js adopt plans/route-reset-distribution-recovery --root . --check`, which is non-mutating: output {"kind":"checked","plan":"route-reset-distribution-recovery","profile":"bug-fix","proposals":["P-01","P-02"]}, exit code 0. Confirmed via git status that only files under plans/route-reset-distribution-recovery/ were created and no file under .aios/ was touched by this work (the one untracked .aios/ file present was already untracked before this task began).
<!-- /aios:attempt-frame v1 number=1 -->
