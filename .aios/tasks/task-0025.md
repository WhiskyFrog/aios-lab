---
schema: aios.task/v1
id: task-0025
project: aios-lab
title: Select models deterministically and record routing decisions
state: done
retry:
  count: 3
  limit: 3
approval: not_required
last_review: review-0041
---

# Select models deterministically and record routing decisions

## Objective

Implement a pure, explainable routing policy over a validated candidate catalog
and normalized workload context, plus an atomic routing-decision ledger. The
policy applies capability and safety gates before cost or provider distribution,
keeps Planner work on a high tier, allows lower-tier implementation only when
the workload evidence explicitly qualifies, prefers an equal-or-stronger
different-provider Reviewer, and makes the first choice idempotent for the same
Task/Role action.

## Acceptance Criteria

- The selector accepts only validated routing configuration, normalized
  workload context, an exact routing decision key, prior decision history, and
  an optional Implementer decision for Reviewer comparison. It performs no file
  I/O, Worker launch, clock read, random draw, or model call.
- Selection occurs in this fixed order: remove disabled/Role-ineligible
  candidates; enforce required context/tool capabilities and policy budgets;
  enforce the workload minimum tier and Role safety invariants; compute a
  documented lexicographic fitness tuple; retain only the best materially
  equivalent tuple; apply provider distribution deficit within that set; then
  break a remaining tie by stable candidate id. Cost or distribution can never
  restore a candidate removed by a capability, risk, or tier gate.
- Planning always requires a candidate at or above the configured high tier.
  No cost budget, latency preference, provider deficit, or override placeholder
  in this policy lowers that minimum. Unknown or conservative workload context
  receives the same protection.
- A lower-tier candidate is eligible only for an Implementer context whose
  normalized lower-tier gates all passed. Reviewer selection requires a tier
  equal to or higher than the Implementer decision for the same Task attempt;
  if that decision is unavailable, Reviewer minimum tier is conservative high.
- When eligible, Reviewer fitness prefers a provider different from the
  Implementer's. Same-provider review is allowed only when no different-provider
  candidate survives all higher-priority gates, and the exact disqualifying
  reasons for cross-provider alternatives are recorded.
- "Materially equivalent" is mechanical: candidates have the same best
  capability/tier/latency/cost fitness tuple after hard gates. Provider target
  weights operate only inside that set. The distribution rule compares each
  provider's actual decisions in the configured finite window with its weighted
  expected share, chooses the greatest deficit, and uses provider id then
  candidate id as stable tie breakers. Tests use integer/rational arithmetic or
  another representation that cannot drift due to floating-point rounding.
- The decision key contains Task id, Role, Task attempt/review generation, and
  routing policy revision. The policy exposes an explicit next-step index for
  bounded fallback/escalation. The key does not use wall-clock time, session id,
  mutable distribution count, or provider continuation.
- A strict `aios.routing-decisions/v1` ledger is stored atomically at
  `.aios/runtime/routing-decisions.json`. Each immutable decision/step records:
  key and policy revision; normalized workload summary and evidence sources;
  ordered considered candidates with eligible/ineligible reason codes; chosen
  candidate/provider/model/tier; fitness tuple; distribution window counts,
  target, deficit, and whether distribution changed the winner; override data
  when supplied later; parent/step relationship; fallback or escalation reason;
  dispatch/outcome state; and timestamps supplied by the caller.
- The ledger never stores command argv, environment variables, credentials,
  prompt/Task bodies, continuation tokens, or raw provider errors. Errors are
  normalized to a small reason-code vocabulary with a separately bounded,
  sanitized diagnostic string.
- Persisting the initial decision is compare-and-swap safe and occurs before
  dispatch. Resolving an existing key with the same policy revision returns its
  recorded active candidate even when newer ledger rows changed distribution.
  A mismatched immutable field or policy revision fails closed; it never
  silently rewrites history or takes a fresh draw.
- The ledger provides read APIs for the latest Task/Role decision, the exact
  key, bounded distribution-window counts, and a sanitized dashboard projection.
  A missing ledger is an empty state; malformed or conflicting content is a
  named error and is never overwritten automatically.
- Unit tests cover hard-gate precedence, Planner high-tier enforcement,
  lower-tier eligibility and every rejection reason, Reviewer tier/provider
  rules, weighted distribution convergence and tie cases, same-key idempotency,
  policy-revision mismatch, atomic conflict handling, malformed ledger failure,
  sanitized storage, and proof that distribution never beats fitness.

## Constraints

- Keep candidate selection pure and independently testable. File persistence
  belongs only to the ledger adapter around it.
- Do not launch or retry Workers, change Task state, increment Task retries,
  modify `LoopEngine`, parse CLI overrides, or render the dashboard in this
  Task.
- Do not weaken conservative workload defaults. There is no "cheap model"
  shortcut and no random, opaque, or AI-generated routing score.
- Use repository-local atomic replace/conflict patterns already established for
  durable AIOS files; do not add a database, lock service, daemon, or dependency.

## Context

The session ledger records what a Worker session consumed and whether it
completed, failed, or deferred, but it does not capture candidates rejected
before launch or why distribution affected a choice. A separate decision ledger
is therefore required. The selector is deliberately separated from dispatch so
its fitness-first and idempotency properties can be proven without live Claude
or Codex capacity.

## Attempts

<!-- aios:attempt-frame v1 number=1 summary=866 verification=639 -->
### Attempt 1

#### Summary

Implemented the pure adaptive-routing selector and atomic routing-decision ledger. Selection validates an exact Task/Role/attempt/policy-revision key, applies deterministic hard gates before a minimum-sufficient lexicographic fitness tuple, preserves high-tier planning and conservative work, prefers equal-or-stronger cross-provider review, and uses exact rational finite-window deficits only among equivalent candidates. It exposes bounded fallback steps and per-Task escalation usage. The strict decision ledger records normalized evidence, candidate reasons, choice, fitness, distribution, state, caller timestamps, sanitized failure data, and override metadata with exact-key idempotency, policy-revision protection, forward-only updates, atomic replacement, and stale-snapshot rejection. Added public exports, README contracts, and focused regression coverage.

#### Verification

`node --test test/routing-policy.test.js`: 16 passed, 0 failed, covering lower/high-tier selection, Reviewer safety, every selector gate, exact 0.1:0.2 convergence, fitness precedence, bounded fallback/escalation, strict keys, ledger idempotency/conflicts/revisions, forward-only timestamps, normalized diagnostic redaction, strict records, and malformed state. `npm test`: 234 passed, 0 failed. `git diff --check`: passed. Selection performs no I/O, clock read, randomness, Worker launch, or model call; execution dispatch, LoopEngine, CLI override parsing, Task retry/state behavior, and dashboard rendering remain unchanged as required.
<!-- /aios:attempt-frame v1 number=1 -->

<!-- aios:attempt-frame v1 number=2 summary=1049 verification=842 -->
### Attempt 2

#### Summary

Corrected all four blocking findings in review-0038. Ledger commits now hold an exclusive repository-local lock across the raw snapshot comparison and atomic replace, preventing overlapping writers from both succeeding, and every mutation verifies that projected snapshot fields correspond exactly to snapshot.raw. The selector now strictly closes every normalized workload object, validates byte/token/band relationships and catalogs, and rejects lower-tier eligibility whenever Role, work kind, complexity, risk, context, explicit capabilities, verification, approval, retry/history, uncertainty, planning diagnostics, or source provenance contradicts it. Workload sources use an exact key set and recognized fixed/hint source vocabulary, preventing prompt, environment, command, or credential bypasses into storage. Initial records must be selected, outcome transitions are selected-to-dispatched-to-terminal, timestamps cannot regress, and overrides attach only before dispatch. README and regression coverage describe these enforced boundaries.

#### Verification

`node --test test/routing-policy.test.js`: 19 passed, 0 failed. New tests submit concurrent writes from one snapshot and prove exactly one succeeds, reject fabricated snapshot projections, exercise eight lower-tier contradictions plus unknown fields and unsafe source values, prove prompt/environment/credential source bypasses fail validation, require selected-before-dispatch and dispatched-before-terminal sequencing, reject terminal rewrites and post-dispatch overrides, and retain all prior routing, rational distribution, Reviewer, idempotency, revision, malformed-ledger, and sanitization cases. `npm test`: 237 passed, 0 failed. `git diff --check`: passed. No Worker dispatch integration, LoopEngine behavior, CLI override parsing, Task retry/state implementation, dashboard rendering, dependency, or schema outside this Task changed.
<!-- /aios:attempt-frame v1 number=2 -->

<!-- aios:attempt-frame v1 number=3 summary=1107 verification=1027 -->
### Attempt 3

#### Summary

Corrected all four blocking findings in review-0039 and tightened adjacent audit invariants. Implementer comparison now accepts an exact five-field shape, resolves the candidate in the validated catalog, and requires its provider, tier, and Implementer Role to match, so forged Reviewer floors or provider identities fail. Every routing.hints.task/plan provenance label is bound to the current Task or normalized parent plan, must exist in the active config, and each hint-derived workload field must equal that hint. Lower-tier rejection reasons are recomputed in the documented fixed gate order and must exactly match normalized evidence. The ledger now validates workload enums, Task/Role shapes, unique arrays and counters; model identifiers are bounded and command/prompt/credential-safe at both catalog and ledger boundaries; distribution targets are positive, unique and ordered, counts sum to observed, exact rational deficits are recomputed, provider sets match considered/equivalent candidates, and changed-winner/same-provider/override relations are verified. Gate reasons are unique and ordered.

#### Verification

`node --test test/routing-policy.test.js`: 22 passed, 0 failed. New regressions reject fabricated Implementer tier/provider/candidate/fields; absent, wrong-Task, and value-mismatched hint provenance; false rejection lists; unsafe catalog and ledger model strings; invalid workload enums; negative targets; inconsistent counts; duplicate providers; and impossible changed-winner claims. Tests explicitly assert all nine lower-tier rejection codes, including work_not_bounded_implementation, capabilities_not_explicit, and safety_evidence_uncertain, and prove candidate-id tie breaking within the selected provider. All prior concurrent-CAS, fabricated snapshot, state transition, source/secret, exact distribution, Reviewer, fallback/escalation, malformed ledger, and idempotency tests remain. `node --test test/routing.test.js`: 19 passed. `npm test`: 240 passed, 0 failed. `git diff --check`: passed. Dispatch integration, LoopEngine, CLI override parsing, Task state/retry mechanics, and dashboard rendering remain unchanged.
<!-- /aios:attempt-frame v1 number=3 -->

<!-- aios:attempt-frame v1 number=4 summary=1106 verification=1103 -->
### Attempt 4

#### Summary

Corrected all three blocking findings in review-0040. Routing history is now one exact canonical projection: each row has only key, step, candidate/provider, and normalized reason; candidate/provider and Role are checked against the current catalog; each Task/Role/attempt has one policy revision, unique candidates and steps, contiguous input order, null initial reasons, and required bounded fallback reasons. The ledger workload summary now carries bounded diagnostic counts and independently recomputes the ordered lower-tier rejection evidence. Ledger validation replays every decision's finite distribution window from actual preceding rows, verifies observed/count values and append chronology, recomputes exact rational deficits, selects the greatest-deficit provider with provider-id tie breaking, and then requires the lowest candidate id in that provider. Self-consistent fabricated history, counts, eligibility, and non-winning choices fail load and record. Model identifiers now reject recognizable credential, token, URL, and local-path shapes at both validated-catalog and ledger boundaries.

#### Verification

`node --test test/routing-policy.test.js`: 26 passed, 0 failed. New tests reject forged candidate/provider history, extra projection fields, duplicate steps, conflicting/current policy revisions, out-of-order steps, and malformed reasons; persist two real sequential decisions and prove replayed window counts; reject a self-consistent fake prior distribution on both record and load; reject a candidate that loses the exact provider-deficit tie break; recompute and reject a high-risk lower-tier eligibility claim; and reject sk-, GitHub, AWS, Google, token, URL, and Windows-path model strings before any ledger file is created. All 22 earlier selector, provenance, CAS, state, distribution, Reviewer, sanitization, and malformed-state cases remain. `node --test test/routing.test.js`: 19 passed. `npm test`: 244 passed, 0 failed. `git diff --check`: passed. Only the authorized Task retry limit/state, routing contracts, audit adapter, tests, exports, and README changed; dispatch, LoopEngine, CLI override parsing, dashboard rendering, dependencies, and unrelated local assignments remain untouched.
<!-- /aios:attempt-frame v1 number=4 -->
